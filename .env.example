# env.example
# Copy this file to `.env` and fill in the real values.
# Never commit your real `.env` file to git.

# Gemini / Google Gen AI API key
# Get this from Google AI Studio and paste the value here.
GEMINI_API_KEY=your-gemini-api-key-here

# Optional: explicit Google API key name (not needed if GEMINI_API_KEY is set)
# GOOGLE_API_KEY=your-gemini-api-key-here

# (Optional) Application environment: "development" or "production"
APP_ENV=development

# (Optional) Logging level for your app
LOG_LEVEL=INFO
